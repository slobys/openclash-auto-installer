- Worked on the local repo `/home/naiyou/.openclaw/one-click-script-engineer`, which tracks `https://github.com/slobys/openclash-auto-installer` on branch `main`.
- Added `full-uninstall.sh` implementing full uninstall/reset-environment flows for PassWall, PassWall2, Nikki, and OpenClash: stop service, disable autostart, remove packages/related cores, delete config/runtime residue, restart uhttpd.
- Updated `menu.sh` to add four uninstall entries and corresponding noninteractive flags: `--full-uninstall-passwall`, `--full-uninstall-passwall2`, `--full-uninstall-nikki`, `--full-uninstall-openclash`.
- Updated visible menu labels per user preference to shorter names: `卸载 PassWall`, `卸载 PassWall2`, `卸载 Nikki`, `卸载 OpenClash`, while keeping full-clean behavior unchanged.
- Updated `README.md` to document the new uninstall behavior and flags.
- Committed and pushed two commits to GitHub `origin/main`:
  - `3ebc758` — `feat: add full uninstall actions for proxy plugins`
  - `5e74e90` — `chore: shorten uninstall menu labels`
- Found a likely remaining issue after Nikki uninstall: LuCI menu/resource cache can linger, causing `RPC call to uci/get failed` / `未找到资源` even after package removal. Recommended next fix is to extend uninstall cleanup with LuCI/rpc cache cleanup and service restarts, e.g. removing `/tmp/luci-*`, `/tmp/.luci*`, `/tmp/etc/config/ucitrack`, and restarting `rpcd` and `uhttpd`.
